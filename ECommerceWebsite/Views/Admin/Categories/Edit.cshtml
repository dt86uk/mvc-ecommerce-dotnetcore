@using ECommerceWebsite.Models.Admin
@model EditCategoryViewModel
@{
    ViewData["Title"] = "Admin Edit Category";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="titleHeader">Edit Category</div>

@using (Html.BeginForm("UpdateCategory", "categories", Model, FormMethod.Post, false, new { enctype = "multipart/form-data", @class = "form" }))
{
    @Html.ValidationSummary(false, string.Empty, new { @class = "required" })

    <div>
        <div class="floatLeft width48perc">
            @Html.HiddenFor(p => p.Id)
            @Html.LabelFor(p => p.CategoryName)
        </div>
        <div class="floatLeft">
            @Html.TextBoxFor(p => p.CategoryName, new { @class = "width200px" })
        </div>
        <div class="clearBoth"></div>
    </div>

    <div class="paddingTopBot10">
        <div class="floatLeft width30perc">
            &nbsp;&nbsp;
        </div>
        <div class="floatLeft">
            <input id="btnSubmit" value="Update Brand" type="submit" class="btnSubmit redOrangeGrad" />
        </div>
        <div class="clearBoth"></div>
    </div>
}